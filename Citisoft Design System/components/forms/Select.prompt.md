Dropdown select styled to match Input; pass `options` as strings or `{value,label}`.

```jsx
<Select label="Category" options={['Steel','Aluminium','Polymers']} />
<Select label="Sort by" options={[{value:'date',label:'Newest'},{value:'price',label:'Lowest price'}]} />
```
