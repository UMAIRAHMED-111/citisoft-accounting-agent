The brand's signature marketing feature row — gradient icon chip beside a title and short body. Use in 2–3 column feature grids.

```jsx
<FeatureChip icon={<GitBranch/>} title="One auditable workflow">
  Every request, quote, and award tracked on a single timeline.
</FeatureChip>
```

- **accent**: `brand` | `rfq`. Keep body copy to a sentence or two.
