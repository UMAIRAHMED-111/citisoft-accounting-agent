Fills text with the signature brand gradient — for one emphasised word in a headline.

```jsx
<h1 style={{fontSize:56,fontWeight:800}}>Quote parts in <GradientText>hours.</GradientText></h1>
```

- Use sparingly — one gradient word per headline. **accent**: `brand` | `rfq`.
